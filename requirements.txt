# LynxAct Coach runtime dependencies (pinned lock).
# Install: pip install -r requirements.txt
flask==3.1.3
python-dotenv==1.2.2  # was 1.0.1 — PYSEC-2026-2270 (fix: 1.2.2); load_dotenv API unchanged
requests==2.34.2
websocket-client==1.9.0
